# PINN 项目源码说明

## 1. 项目定位

本项目围绕二维声波方程的 Physics-Informed Neural Network（PINN）求解展开，核心目标是比较并改进以下几类模型在不同速度场数据集上的前向建模能力：

- 传统/早期 PINN 基线；
- `ST-PINN`：显式引入时间序列建模与时空融合；
- `KAN-PINN`：用 KAN 取代普通 MLP 作为空间主干；
- `KAN-ST-PINN`：空间 KAN + 时间 LSTM/Attention 的主力模型。

项目当前的实验对象主要有 3 组数据集：

- `threelayer`：三层速度模型；
- `marmousi`：合成 Marmousi 风格复杂速度场；
- `overthrust`：合成 Overthrust 风格复杂速度场。

整体技术路线是：

1. 先用有限差分求解器生成参考波场 `p_ref`；
2. 再把 `(x, y, t) -> p` 学成 PINN / KAN-PINN / ST-PINN / KAN-ST-PINN；
3. 用 PDE 残差、初值损失、边界损失、监督数据损失联合训练；
4. 在 `trained/` 下保存模型，在 `figures/` 下保存论文图和诊断图；
5. 用若干评估脚本计算 `Rel L2`、逐时刻误差并生成对比图。

---

## 2. 目录结构与分层

### 2.1 目录结构

```text
PINN/
├─ main.py                       # 实验总调度入口
├─ train_pinn_forward.py         # 早期/基线 PINN 前向求解训练脚本
├─ train_st_pinn.py              # ST-PINN 训练与消融
├─ train_kan_pinn.py             # KAN-PINN / KAN-ST-PINN 主训练脚本
├─ finetune.py                   # R6 微调脚本
├─ compute_l2.py                 # 统一 Rel L2 评估
├─ evaluate.py                   # R6 评估与 R5 基线对比
├─ vis_compare.py                # 论文图/总对比图生成
├─ cmp_t2.py                     # t=2.0s 单时刻对比
├─ diag_latetime.py              # 晚时刻误差诊断
├─ hist_analysis.py              # 历史 loss 分析（带硬编码路径）
├─ plot_abl.py                   # 拼图式消融图生成（带硬编码路径）
├─ plot_3layer.py                # threelayer 波场旧风格绘图
├─ run_r6.sh                     # 服务器批量启动 R6 的旧脚本
├─ PINNs_util/
│  ├─ PINNs_aux.py               # PDE/采样/边界条件/旧模型与辅助组件
│  ├─ PINNs_fdiff.py             # 有限差分求解器
│  ├─ PINNs_plots.py             # 老版交互式绘图函数
│  ├─ datasets.py                # Marmousi / Overthrust 数据集生成
│  ├─ st_pinn.py                 # STPinn 主模型定义
│  └─ kan_pinn.py                # KANLayer / KANPinn / KANSTPinn
├─ trained/                      # 训练产物目录
├─ figures/                      # 图片输出目录
├─ 2502.17134v2.pdf              # 参考论文/资料
└─ 研究内容与需求1.docx            # 需求文档
```

### 2.2 分层理解

项目基本可以分成 3 层：

- 实验编排层：`main.py`
- 训练/评估/出图层：根目录各类 `train_*`、`compute_l2.py`、`vis_compare.py` 等
- 底层模型与数值工具层：`PINNs_util/`

### 2.3 核心数据流

最常见的数据流如下：

1. `PINNs_util/PINNs_fdiff.py` 用有限差分生成参考解；
2. `PINNs_util/datasets.py` 或训练脚本内的 `prepare_*` 函数把参考解整理成训练数据；
3. `PINNs_util/PINNs_aux.py` 负责生成 collocation 点、边界点、PDE 残差、边界残差；
4. `PINNs_util/st_pinn.py` 或 `PINNs_util/kan_pinn.py` 定义模型；
5. `train_st_pinn.py` / `train_kan_pinn.py` 实施训练并保存到 `trained/`；
6. `compute_l2.py` / `evaluate.py` 做误差评估；
7. `vis_compare.py`、`plot_3layer.py`、`cmp_t2.py` 等生成图像到 `figures/`。

---

## 3. 文件之间的调用关系

### 3.1 主线关系

```text
main.py
├─ train_kan_pinn.py
│  ├─ PINNs_util/kan_pinn.py
│  ├─ PINNs_util/datasets.py
│  ├─ PINNs_util/PINNs_aux.py
│  └─ PINNs_util/PINNs_fdiff.py
├─ train_st_pinn.py
│  ├─ PINNs_util/st_pinn.py
│  ├─ PINNs_util/datasets.py
│  ├─ PINNs_util/PINNs_aux.py
│  └─ PINNs_util/PINNs_fdiff.py
├─ finetune.py
│  └─ 复用 train_kan_pinn.py 的训练/评估函数
├─ compute_l2.py
│  ├─ PINNs_util/kan_pinn.py
│  ├─ PINNs_util/datasets.py
│  ├─ PINNs_util/PINNs_aux.py
│  └─ PINNs_util/PINNs_fdiff.py
└─ vis_compare.py
   └─ 复用评估逻辑，自行重建数据和模型后出图
```

### 3.2 历史/旁路关系

- `train_pinn_forward.py` 是较早的通用 PINN 训练脚本，依赖 `PINNs_aux.py` 中的旧模型定义，例如 `FCN`、`FCN_with_Attention`、`SpaceTimeAttentionLSTM`、`LongHistoryTemporalEncoderPINN`。
- `hist_analysis.py`、`plot_abl.py`、`run_r6.sh` 带有明显的旧环境路径或旧脚本名，更像“实验当时的辅助脚本”，不是当前最稳妥的统一入口。

---

## 4. 根目录脚本逐文件说明

## 4.1 `main.py`

### 功能定位

项目的实验流水线调度器，用于按顺序执行训练、微调、评估和绘图。

### 主要功能

- 封装 `run_cmd()` 统一运行子进程；
- 定义流水线步骤：
  - `train_kan_st`
  - `train_kan`
  - `train_st`
  - `finetune`
  - `evaluate`
  - `visualize`
- 支持完整流程、只评估、只微调、快速测试等模式。

### 输入

- 命令行参数：
  - `--steps`
  - `--epochs`
  - `--finetune-epochs`
  - `--quick`
  - `--eval-only`
  - `--finetune-only`

### 输出

- 不直接产出模型，而是调用其它脚本间接生成：
  - `trained/` 下模型文件；
  - `figures/` 下图像；
  - 终端日志。

### 与其它文件的关系

- 调用 `train_kan_pinn.py`、`train_st_pinn.py`、`finetune.py`、`compute_l2.py`、`vis_compare.py`、`plot_abl.py`。

### 备注

- 当前代码里 `TRAIN_DIR = ROOT_DIR/PINNs_util`，但被调用的 `train_kan_pinn.py`、`train_st_pinn.py`、`finetune.py` 实际位于项目根目录，不在 `PINNs_util/` 下。
- 因此它更像“意图上的总入口”，直接运行前建议先确认路径配置。

---

## 4.2 `train_pinn_forward.py`

### 功能定位

较早的前向 PINN 训练脚本，承担“基础 PINN + 多种历史模型尝试”的角色。

### 主要功能

- 定义大而全的 `Config`；
- 自建三层速度模型和高斯脉冲初值；
- 调用有限差分求参考解；
- 支持多种旧模型：
  - `FCN`
  - `FCN_with_Attention`
  - `FCN_Attention`
  - `LSTMAttention`
  - `SpaceTimeAttentionLSTM`
  - `LongHistoryTemporalEncoderPINN`
- 定义训练阶段权重、因果训练、可选自适应损失权重；
- 提供模型保存、最佳模型保存、模型加载、评估与全套可视化输出。

### 输入

- 主要来自 `Config`，不是命令行驱动；
- 输入数据由脚本内部生成：
  - 初始条件 `I_func`
  - 速度场 `get_wave_speed`
  - 参考波场 `p_ref`

### 输出

- `trained/forward/` 下的模型和训练日志；
- `figures/` 下的速度场图、动画、比较图、loss 曲线等。

### 与其它文件的关系

- 强依赖 `PINNs_util/PINNs_aux.py` 中的工具函数和老模型；
- 依赖 `PINNs_util/PINNs_fdiff.py` 生成参考解。

### 适合如何理解

- 这是“老基线 + 历史试验田”。
- 如果要理解项目最初是如何从普通 PINN 演化到时空增强模型的，这个文件很关键。
- 如果要跑当前主线实验，它不是首选入口。

---

## 4.3 `train_st_pinn.py`

### 功能定位

`ST-PINN` 主训练脚本，同时也是时空模块消融实验的统一入口。

### 主要功能

- 用 `Config` 配置 ST-PINN 结构和训练超参数；
- 支持 3 套数据集：
  - `threelayer`
  - `marmousi`
  - `overthrust`
- 定义 `ABLATION_VARIANTS`，可做以下消融：
  - `full`
  - `no_fourier`
  - `no_lstm`
  - `no_attention`
  - `no_lstm_attn`
  - `self_attn`
- 提供训练、评估、保存 loss 曲线、保存波场对比图。

### 输入

- 命令行参数：
  - `--dataset`
  - `--variant`
  - `--ablation`
  - `--epochs`
  - `--lr`
  - `--nx`
  - `--T`
  - `--w-pde`
  - `--w-bc`
  - `--n-colloc`
  - `--d-model`
  - `--pde-warmup`
  - `--pde-rampup`
  - `--quick`
  - `--no-adaptive`
  - `--no-data`

### 输出

- 模型目录：`trained/<dataset>/<variant>/`
  - `model.pt`
  - `history.pt`
- 图像目录：`figures/<dataset>/<variant>/`
  - `loss_curves.png`
  - `comparison.png`
- 若 `--ablation`，还会在 `figures/<dataset>/` 下写 `ablation_summary.txt`。

### 与其它文件的关系

- 使用 `PINNs_util/st_pinn.py` 里的 `STPinn`；
- 使用 `PINNs_util/datasets.py` 生成复杂速度场数据；
- 使用 `PINNs_util/PINNs_aux.py` 做 PDE、边界、采样；
- 使用 `PINNs_util/PINNs_fdiff.py` 生成 threelayer 参考解。

### 关系定位

- 它是“ST-PINN 体系”的核心训练文件；
- 同时也是 `main.py` 中 `train_st` 步骤的目标脚本。

---

## 4.4 `train_kan_pinn.py`

### 功能定位

当前项目最核心的训练脚本，负责训练：

- `KAN-PINN`（纯 KAN 空间主干）
- `KAN-ST-PINN`（KAN + BiLSTM + Cross-Attention）

### 主要功能

- 定义 `Config`；
- 支持三套数据集；
- 根据 `pure_kan` 选择：
  - `KANPinn`
  - `KANSTPinn`
- 引入比 `train_st_pinn.py` 更强的晚时刻采样/加权策略：
  - `sample_data_time_weighted`
  - `_pregenerate_colloc_pool`
  - `late_data_alpha`
  - `late_colloc_uniform`
- 支持断点续训 `--resume`；
- 保存 checkpoint、最终模型、history、图像。

### 输入

- 命令行参数：
  - `--dataset`
  - `--epochs`
  - `--lr`
  - `--nx`
  - `--T`
  - `--w-pde`
  - `--w-bc`
  - `--n-colloc`
  - `--d-model`
  - `--kan-layers`
  - `--grid-size`
  - `--spline-order`
  - `--pure-kan`
  - `--pde-warmup`
  - `--pde-rampup`
  - `--quick`
  - `--no-data`
  - `--resume`

### 输出

- 纯 KAN 时：
  - `trained/<dataset>/kan_pinn/`
  - `figures/<dataset>/kan_pinn/`
- KAN-ST 时：
  - `trained/<dataset>/kan_st_pinn/`
  - `figures/<dataset>/kan_st_pinn/`
- 典型文件包括：
  - `checkpoint_epoch*.pt`
  - `model.pt`
  - `history.pt`
  - `loss_curves.png`
  - `comparison.png`

### 与其它文件的关系

- 依赖 `PINNs_util/kan_pinn.py` 中的 `KANPinn` / `KANSTPinn`；
- 依赖 `PINNs_util/datasets.py`；
- 依赖 `PINNs_util/PINNs_aux.py`；
- 被 `finetune.py`、`evaluate.py` 复用其训练/评估逻辑。

### 关系定位

- 这是当前项目最重要的主线训练文件；
- 如果只能读一个训练脚本，应优先读它。

---

## 4.5 `finetune.py`

### 功能定位

R6 微调脚本，用已有 R5 权重继续训练，重点提升晚时刻误差表现。

### 主要功能

- 定义 `R6Config(Config)`，在 `train_kan_pinn.Config` 基础上覆写：
  - 更低学习率；
  - 更高 `n_data` / `n_colloc` / `n_bc`；
  - PDE 直接开启；
  - 更强晚时刻加权；
  - 更紧的因果参数；
- 从 `trained/<dataset>/<src_tag>/` 加载旧权重；
- 调用 `train_kan_pinn.py` 中的 `train_model()` 继续训练；
- 重新评估并出图。

### 输入

- 命令行参数：
  - `--dataset`
  - `--pure-kan`
  - `--epochs`
  - `--n-data`
  - `--n-colloc`

### 输出

- 输出到：
  - `trained/<dataset>/kan_st_pinn_r6/`
  - `trained/<dataset>/kan_pinn_r6/`
- 图像输出到：
  - `figures/<dataset>/kan_st_pinn_r6/`
  - `figures/<dataset>/kan_pinn_r6/`

### 与其它文件的关系

- 直接复用 `train_kan_pinn.py` 的：
  - `prepare_dataset`
  - `align_temporal_config`
  - `prepare_initial_condition`
  - `create_model`
  - `train_model`
  - `evaluate_model`
  - `save_loss_curves`
  - `save_comparison_plot`

### 关系定位

- 它不是独立模型，而是“基于 `train_kan_pinn.py` 的后处理训练阶段”。

---

## 4.6 `compute_l2.py`

### 功能定位

统一评估所有 KAN 系列实验的全局 `Rel L2`、`MAE` 和逐时刻误差。

### 主要功能

- 重建数据集和模型结构；
- 自动对齐 `seq_len` 与 `dt_hist`；
- 自动加载 `model.pt` 或最近 checkpoint；
- 计算：
  - 全局 `rel_l2`
  - `mae`
  - `per_timestep`
  - `mean_per_t`
  - `max_per_t`
- 汇总打印表格。

### 输入

- 默认扫描 `trained/` 下以下实验：
  - `kan_st_pinn/threelayer`
  - `kan_st_pinn/marmousi`
  - `kan_st_pinn/overthrust`
  - `kan_pinn/threelayer`
  - `kan_pinn/marmousi`
  - `kan_pinn/overthrust`

### 输出

- 主要是终端输出；
- 不负责保存大规模图像。

### 与其它文件的关系

- 评估逻辑后续被 `cmp_t2.py`、`diag_latetime.py`、`plot_3layer.py` 复用部分函数。

### 关系定位

- 它是“统一评测脚本”，适合在训练完成后快速确认指标。

---

## 4.7 `evaluate.py`

### 功能定位

专门面向 R6 的评估脚本，把 R6 微调结果和 R5 基线做对比，并给出 PASS/MARGINAL/FAIL 判定。

### 主要功能

- 维护 `R5_BASELINE`；
- 评估 `kan_st_pinn_r6` 和 `kan_pinn_r6`；
- 可选等待所有模型训练完毕再评估；
- 输出总体结论 `SUCCESS / NEEDS_ITERATION / MARGINAL`；
- 把结果写入 `r6_results.json`。

### 输入

- 命令行参数：
  - `--wait`
  - `--max-wait`
  - `--poll`

### 输出

- `r6_results.json`
- 终端评估报告。

### 与其它文件的关系

- 复用 `train_kan_pinn.py` 的数据准备与建模函数；
- 复用 `finetune.py` 中的 `R6Config`。

### 关系定位

- 它是“R6 实验验收脚本”，不是通用评估脚本。

---

## 4.8 `vis_compare.py`

### 功能定位

论文级综合可视化脚本，用于统一生成损失曲线、波场对比图、消融柱状图、逐时刻误差图和文字表格。

### 主要功能

- `fig1_loss_curves()`：三种模型 loss 曲线对比；
- `fig2_wavefield()`：KAN-ST-PINN 在各数据集上的参考/预测/误差图；
- `fig3_ablation_bars()`：全局 `Rel L2` 柱状图；
- `fig4_per_timestep()`：逐时刻误差曲线；
- `fig5_ablation_table()`：生成文字表格与总结。

### 输入

- 依赖 `trained/` 下已有模型与 history；
- 自行生成数据集，不依赖训练过程中的缓存。

### 输出

- 输出到 `figures/paper/`；
- 文件名形如：
  - `fig1_loss_curves.png`
  - `fig2_wavefield_<dataset>.png`
  - `fig3_ablation_rel_l2.png`
  - `fig4_per_timestep_rel_l2.png`
  - `ablation_summary.txt`

### 与其它文件的关系

- 与 `compute_l2.py` 类似，也会重建模型和数据；
- 但重点不在数值评估，而在论文图输出。

### 关系定位

- 它是“最终展示层”的核心脚本。

---

## 4.9 `cmp_t2.py`

### 功能定位

针对 `threelayer` 数据集，在 `t=2.0s` 单时刻上对比 KAN-ST 与 Pure-KAN。

### 主要功能

- 加载两种模型；
- 找到离 `2.0s` 最近的时间索引；
- 计算该时刻的 `Rel L2`；
- 输出 2x3 面板图：
  - Reference
  - KAN-ST Prediction
  - KAN-ST Error
  - Reference
  - Pure-KAN Prediction
  - Pure-KAN Error
- 同时写文本指标。

### 输入

- `trained/threelayer/kan_st_pinn`
- `trained/threelayer/kan_pinn`

### 输出

- `figures/paper/fig_t2_compare_threelayer_r5.png`
- `figures/paper/t2_rel_l2_threelayer_r5.txt`

### 关系定位

- 它是“单一关键时刻”的专项比较脚本。

---

## 4.10 `diag_latetime.py`

### 功能定位

专门分析 KAN-ST-PINN 在晚时刻误差偏大的原因。

### 主要功能

- 逐时刻计算：
  - `Rel L2`
  - 平均绝对误差
  - 最大绝对误差
  - 参考场能量
  - 参考场最大振幅
- 打印关键采样点；
- 输出 2x2 诊断图，分析“参考信号衰减”和“相对误差放大”的关系。

### 输入

- `trained/threelayer/kan_st_pinn`

### 输出

- `figures/paper/diagnostic_r5_latetime.png`

### 关系定位

- 它是“误差解释工具”，帮助回答为什么后期时刻 `Rel L2` 看起来更差。

---

## 4.11 `hist_analysis.py`

### 功能定位

针对单次训练历史的 loss 分析脚本，兼做最后 checkpoint 的评估。

### 主要功能

- 读取指定 `history.pt`；
- 输出 loss 的首尾值、最小值、里程碑值；
- 画总 loss、分项 loss、末段 PDE loss 等图；
- 再加载模型，输出当前 `Rel L2` 和 `MAE`；
- 生成比较图。

### 输入

- 当前脚本写死了：
  - `HIST_PATH`
  - `CKPT_DIR`
  - `OUT_DIR`

### 输出

- `loss_analysis.png`
- 比较图
- 终端分析日志。

### 备注

- 这是一个明显绑定个人环境的分析脚本；
- 直接复用前需要改盘符和目录。

### 关系定位

- 更像“实验后验分析工具”，不是项目主线入口。

---

## 4.12 `plot_abl.py`

### 功能定位

把已有图片再次拼接排版，生成面向展示的消融图。

### 主要功能

- 读取现成图像而非重新推理；
- 裁切三行波场图；
- 拼成 4 行布局：
  - Reference
  - KAN-ST Prediction
  - Pure-KAN Prediction
  - Per-timestep Rel L2 柱状图

### 输入

- 当前脚本硬编码读取：
  - `H:\wusheng\16\figures\final`
  - `H:\wusheng\16\figures\paper_r6`

### 输出

- `H:\wusheng\16\figures\ablation\ablation_<dataset>.png`

### 备注

- 柱状图中的 `rel_l2` 和 `per_t` 数值是脚本内手工写入，不是运行时即时计算；
- 因此它更偏“排版脚本”，不是评估真源。

---

## 4.13 `plot_3layer.py`

### 功能定位

对 `threelayer` 数据集生成旧风格论文图，聚焦 KAN-ST-PINN 的 R5 结果。

### 主要功能

- 加载 `kan_st_pinn` 模型；
- 选取多个时刻；
- 输出 3 行图：
  - Reference
  - KAN-ST-PINN
  - Error
- 同时写每个选取时刻的 `Rel L2` 文本。

### 输入

- `trained/threelayer/kan_st_pinn`

### 输出

- `figures/paper/fig2_wavefield_threelayer_r5_oldstyle.png`
- `figures/paper/fig2_wavefield_threelayer_r5_oldstyle_rel.txt`

### 关系定位

- 它是 `vis_compare.py` 之外的一个“旧版专项绘图脚本”。

---

## 4.14 `run_r6.sh`

### 功能定位

面向 Linux 服务器的批量实验启动脚本，用于并行启动 6 组 R6 微调实验。

### 主要功能

- 清理旧 R6 目录；
- 按 GPU 编号同时启动：
  - 3 个 KAN-ST-PINN
  - 3 个 Pure-KAN
- 用 `nohup` 后台运行并写日志。

### 输入

- 依赖 Linux 服务器环境；
- 依赖多 GPU；
- 依赖 `/root/PINNs_util` 目录结构。

### 输出

- `/root/r6_*.log`
- R6 模型目录。

### 备注

- 其中调用的是 `finetune_r6.py`，而当前仓库实际文件名是 `finetune.py`；
- 因此它属于历史脚本，直接运行前需要修正。

---

## 5. `PINNs_util/` 模块逐文件说明

## 5.1 `PINNs_util/PINNs_fdiff.py`

### 功能定位

二维声波方程的有限差分参考求解器，用来生成训练/评估所需的“真值”波场。

### 主要函数

- `solver(I, c, Lx, Ly, Nx, Ny, dt, T, dtype=np.float32)`
- `advance(u, u_n, u_nm1, q_a, dx2, dy2, dt2, step1=False)`

### 输入

- `I`：初始条件函数；
- `c`：速度场函数；
- `Lx, Ly`：空间范围；
- `Nx, Ny`：网格数；
- `dt`：时间步长，若为负则把绝对值当作 CFL 安全系数；
- `T`：总时长。

### 输出

- `u_log`：完整时空波场；
- `xx, yy`：网格坐标；
- `t`：时间轴；
- `dt`：最终实际步长。

### 与其它文件的关系

- 被 `train_pinn_forward.py`、`train_st_pinn.py`、`train_kan_pinn.py`、`compute_l2.py`、`vis_compare.py`、`datasets.py` 等多处调用。

### 关系定位

- 它是所有训练和评估的“参考解源头”。

---

## 5.2 `PINNs_util/datasets.py`

### 功能定位

生成 Marmousi 和 Overthrust 两类复杂速度场数据集，并统一封装成训练/评估可直接使用的数据字典。

### 主要函数

- `_marmousi_velocity()`
- `_overthrust_velocity()`
- `_build_dataset()`
- `generate_marmousi()`
- `generate_overthrust()`

### 输入

- `device`
- `Nx, Ny`
- 内部定义的物理尺度与高斯脉冲位置。

### 输出

统一返回字典，典型字段包括：

- `L`, `T`
- `L_phys`, `T_phys`
- `p_ref`
- `xx`, `yy`, `t`
- `xy`
- `r_ref`
- `n_T`, `n_L`
- `c_func`
- `p_max`

### 与其它文件的关系

- 被 `train_st_pinn.py`、`train_kan_pinn.py`、`compute_l2.py`、`vis_compare.py` 调用。

### 关系定位

- 它是“复杂数据集生成层”。

---

## 5.3 `PINNs_util/st_pinn.py`

### 功能定位

定义当前项目的 `STPinn` 主模型，即 ST-PINN 的网络结构。

### 主要类

- `ResBlock`
- `STPinn`

### 模型结构概括

`STPinn` 的前向过程可以理解为：

1. 把输入 `(x, y, t)` 分成空间和时间两路；
2. 对空间和时间分别做 Fourier Feature；
3. 空间分支用残差 MLP 编码；
4. 时间分支把当前时刻扩成过去时间窗口，用 BiLSTM 编码；
5. 再用 cross-attention 或 self-attention 做时空融合；
6. 最后由 MLP head 输出标量波场值。

### 输入输出

- 输入：`r`，形状 `[B, 3]`
- 输出：`out`，形状 `[B, 1]`

### 与其它文件的关系

- 只被 `train_st_pinn.py` 主线调用；
- 对应 ST-PINN 的正式模型定义。

### 关系定位

- 它是“ST-PINN 主干”。

---

## 5.4 `PINNs_util/kan_pinn.py`

### 功能定位

定义 KAN 系列模型，是当前项目最关键的模型文件之一。

### 主要类

- `KANLayer`
- `KANPinn`
- `KANSTPinn`

### 各类职责

#### `KANLayer`

- 单层 KAN；
- 核心由：
  - B 样条基函数；
  - 样条系数；
  - 残差线性通路；
 组成。

#### `KANPinn`

- 纯 KAN-PINN；
- 把 `(x,y,t)` 经过 Fourier 映射后，送入多层 KAN，再输出 `p`；
- 不显式建模时间历史窗口。

#### `KANSTPinn`

- KAN-ST-PINN；
- 空间分支使用 KAN；
- 时间分支使用时间窗口 + BiLSTM；
- 之后用 cross-attention 融合；
- 最后再用 KAN head 输出。

### 输入输出

- 输入统一是 `[B, 3]`
- 输出统一是 `[B, 1]`

### 与其它文件的关系

- 被 `train_kan_pinn.py`、`compute_l2.py`、`vis_compare.py` 调用。

### 关系定位

- 它是“当前主线模型库”。

---

## 5.5 `PINNs_util/PINNs_aux.py`

### 功能定位

这是一个历史累积型辅助模块，既包含底层训练工具，也包含多种早期/备选模型定义。

### 当前仍然重要的函数

- `pde_residual(p, r, c)`
  - 计算二维声波方程残差；
- `update_lambda(model, loss_lst, lamb_lst, alpha)`
  - 根据梯度范数自适应更新损失权重；
- `xyt_tensor(rxy, t, device)`
  - 把空间点和时间轴展开成 `[N,3]` 输入张量；
- `rand_colloc()`
- `rand_colloc_fixed()`
- `rand_colloc_mixed()`
  - 生成 collocation 点；
- `rand_boundary()`
  - 生成边界点；
- `bc_residual_absorbing()`
  - 计算吸收边界条件残差。

### 历史/实验性模型

该文件还定义了很多模型和组件：

- `SpaceTimeAttentionLSTM`
- `FactorizedSTBlock`
- `LSTMAttention`
- `Attention`
- `CrossAttention`
- `MLP`
- `FCN_Attention`
- `FCN`
- `FCN_with_Attention`
- `LongHistoryTemporalEncoderPINN`

### 与其它文件的关系

- `train_pinn_forward.py` 会大量使用这里的旧模型；
- `train_st_pinn.py`、`train_kan_pinn.py` 主要使用这里的“工具函数”，而不是主要模型定义。

### 关系定位

- 可以把它理解成“历史模型仓库 + 通用训练工具箱”。

---

## 5.6 `PINNs_util/PINNs_plots.py`

### 功能定位

老版绘图工具集，偏交互式/Jupyter 风格。

### 主要函数

- `plot_field()`
- `plot_data()`
- `plot_estimation()`
- `plot_train_log()`
- `plot_speed()`
- `plot_speed_log()`
- `plot_comparison()`

### 当前项目中的地位

- 更多是历史辅助工具；
- 当前主线脚本大多已经在各自文件中实现了专门的保存型绘图函数，而不是直接用它。

### 关系定位

- 它是“老版可视化函数库”。

---

## 6. 训练产物与图像产物说明

## 6.1 `trained/`

这是训练输出目录，常见结构如下：

```text
trained/
└─ <dataset>/
   ├─ kan_st_pinn/
   ├─ kan_pinn/
   ├─ kan_st_pinn_r6/
   ├─ kan_pinn_r6/
   └─ <variant>/
```

常见文件包括：

- `model.pt`：最终模型权重；
- `history.pt`：训练历史；
- `checkpoint_epoch*.pt`：中间 checkpoint；
- `status.txt`：部分脚本/实验可能生成的状态文件。

## 6.2 `figures/`

这是图像输出目录，常见内容有：

- `comparison.png`：参考/预测/误差对比图；
- `loss_curves.png`：训练曲线；
- `figures/paper/`：论文图；
- `figures/final/`、`figures/ablation/`、`figures/paper_r6/`：实验阶段不同用途的图片。

---

## 7. 当前项目里“新旧代码并存”的理解方式

阅读这个仓库时，建议按下面方式区分：

### 第一组：当前主线

- `train_kan_pinn.py`
- `finetune.py`
- `compute_l2.py`
- `vis_compare.py`
- `PINNs_util/kan_pinn.py`
- `PINNs_util/datasets.py`
- `PINNs_util/PINNs_fdiff.py`
- `PINNs_util/PINNs_aux.py` 中的工具函数部分

### 第二组：ST-PINN 专线

- `train_st_pinn.py`
- `PINNs_util/st_pinn.py`

### 第三组：老基线/历史试验

- `train_pinn_forward.py`
- `PINNs_util/PINNs_aux.py` 中的旧模型类
- `PINNs_util/PINNs_plots.py`

### 第四组：实验后处理和临时脚本

- `cmp_t2.py`
- `diag_latetime.py`
- `hist_analysis.py`
- `plot_abl.py`
- `plot_3layer.py`
- `run_r6.sh`

---

## 8. 推荐阅读顺序

如果要快速理解项目，建议按这个顺序看：

1. `train_kan_pinn.py`
2. `PINNs_util/kan_pinn.py`
3. `PINNs_util/datasets.py`
4. `PINNs_util/PINNs_aux.py`
5. `compute_l2.py`
6. `vis_compare.py`
7. `train_st_pinn.py`
8. `PINNs_util/st_pinn.py`
9. `train_pinn_forward.py`

原因是：

- 先看主线训练脚本，最快知道项目现在真正怎么跑；
- 再看主线模型定义，理解网络结构；
- 然后看数据集和 PDE 工具，理解输入数据怎么来；
- 最后再看历史脚本，理解项目如何演化。

---

## 9. 运行入口建议

### 最推荐的主线训练入口

```bash
python train_kan_pinn.py --dataset threelayer
python train_kan_pinn.py --dataset marmousi
python train_kan_pinn.py --dataset overthrust
```

### 纯 KAN 对照实验

```bash
python train_kan_pinn.py --dataset threelayer --pure-kan
```

### ST-PINN 消融

```bash
python train_st_pinn.py --dataset threelayer --ablation
```

### 微调

```bash
python finetune.py --dataset threelayer
python finetune.py --dataset threelayer --pure-kan
```

### 统一评估

```bash
python compute_l2.py
python evaluate.py
```

### 统一出图

```bash
python vis_compare.py
```

---

## 10. 代码层面需要特别注意的点

### 10.1 路径硬编码

以下文件包含明显的硬编码路径，迁移环境时要先改：

- `hist_analysis.py`
- `plot_abl.py`
- `run_r6.sh`

### 10.2 历史脚本名与当前文件名不一致

- `run_r6.sh` 中调用 `finetune_r6.py`，当前仓库实际文件名为 `finetune.py`。

### 10.3 调度器路径可能不一致

- `main.py` 中 `TRAIN_DIR` 指向 `PINNs_util`，但训练脚本位于项目根目录。

### 10.4 同类逻辑在多个评估脚本中有重复

- `compute_l2.py`、`vis_compare.py`、`cmp_t2.py`、`diag_latetime.py` 都各自维护了部分“重建数据/模型/推理”的逻辑；
- 这让脚本更独立，但也增加了后续维护成本。

---

## 11. 一句话总结每个代码文件

- `main.py`：统一调度训练、微调、评估和出图。
- `train_pinn_forward.py`：早期通用 PINN 基线训练脚本。
- `train_st_pinn.py`：ST-PINN 训练与结构消融。
- `train_kan_pinn.py`：当前主线训练脚本，负责 KAN-PINN 与 KAN-ST-PINN。
- `finetune.py`：R6 微调。
- `compute_l2.py`：统一数值评估。
- `evaluate.py`：R6 相对 R5 的验收评估。
- `vis_compare.py`：论文图与综合可视化。
- `cmp_t2.py`：t=2.0s 单时刻对比。
- `diag_latetime.py`：晚时刻误差诊断。
- `hist_analysis.py`：单次实验历史分析。
- `plot_abl.py`：基于已有图像进行消融排版。
- `plot_3layer.py`：threelayer 旧风格出图。
- `run_r6.sh`：服务器批量启动 R6 的历史脚本。
- `PINNs_util/PINNs_fdiff.py`：有限差分真值求解器。
- `PINNs_util/datasets.py`：复杂速度场数据集构造。
- `PINNs_util/st_pinn.py`：ST-PINN 模型定义。
- `PINNs_util/kan_pinn.py`：KAN 系列模型定义。
- `PINNs_util/PINNs_aux.py`：PDE/采样/边界工具与历史模型集合。
- `PINNs_util/PINNs_plots.py`：老版绘图函数库。

